// Google Fonts werden via <link> im HTML geladen
// Du kannst diese Datei nutzen wenn du zusätzliche Font-Features brauchst

window.addEventListener('DOMContentLoaded', () => {
  // Fonts sind bereits geladen via CSS link tag
  console.log('Fonts ready');
});
